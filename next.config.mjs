/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the Next.js dev indicator (bottom-left) so it doesn't overlap our Calc FAB
  devIndicators: false,
};

export default nextConfig;
