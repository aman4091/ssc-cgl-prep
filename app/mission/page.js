"use client";

import MissionToday from "@/components/MissionToday";

// /mission — aaj ka din. Homepage bhi yahi dikhata hai (upar), ye uska apna ghar.
export default function MissionPage() {
  return <MissionToday />;
}
