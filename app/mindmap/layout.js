export const metadata = {
  title: "Mind Maps — SSC CGL",
  description: "History, Polity, Geography… ke mind maps se recall cards — raasta poora, jawab ek.",
};

export default function MindMapLayout({ children }) {
  return children;
}
