export const metadata = {
  title: "CA Revision — SSC CGL",
  description: "Current Affairs magazine se atomic cards — Recall, SRS, 1 Oct tak.",
};

export default function CaRevisionLayout({ children }) {
  return children;
}
